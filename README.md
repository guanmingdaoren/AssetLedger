# 设备资产台账

一个使用 PySide6 构建的本地桌面资产管理程序。所有设备当前信息、分类字典、系统设置和字段级变更历史均保存在同一个 Excel 工作簿中。

## 更新记录

更新记录按日期倒序维护。每次发布功能改进或修复时，应在本节顶部补充日期和用户可感知的变化。

### 2026-06-28：筛选条件精简

- 左侧筛选条件移除 `全部管理人` 和 `全部取得方式` 两个下拉框，保留使用状态、位置、品牌和使用人筛选。
- 管理人和取得方式仍作为设备字段保存、搜索和展示，只是不再占用左侧筛选区域。
- 刷新、清除筛选和使用人未分配筛选逻辑同步调整。

### 2026-06-23：设备删除功能

- 新增设备物理删除功能，可从顶部工具栏或设备列表右键菜单执行。
- 删除设备时会同时移除 `资产台账` 中的设备行、关联的 `存储介质` 行以及该设备对应的 `变更历史`。
- 删除前会显示二次确认，列出设备器材、资产UID、bm编码、资产唯一标识符、存储介质数量和变化记录数量。
- 删除仍使用现有安全写入流程：检查外部修改和文件占用、创建时间戳备份、临时文件原子替换、保存后重载缓存。
- 增加删除相关自动化测试，覆盖关联数据移除、备份创建、外部修改冲突、右键菜单和确认取消行为。

### 2026-06-21：主列表右键菜单

- 中央设备列表新增右键菜单，右键设备行时会先自动选中该设备，再显示常用操作。
- 设备行右键支持 `查看详情`、`查看变化记录`、`编辑设备`、`复制为新设备`、`删除设备` 和 `刷新列表`。
- 右键空白区域时提供 `新增设备` 和 `刷新列表`，方便在列表区域直接操作。
- `查看变化记录` 会自动切换到右侧变化记录页；`复制为新设备` 继续复用现有复制流程，保存后自动选中新设备并高亮提示。
- 新增 GUI 测试覆盖右键菜单项、空白区域菜单、右键行选中和详情/变化记录切换行为。

### 2026-06-19：备注字段与旧工作簿兼容升级

- 工作簿升级为版本 5，资产台账将原 `备注` 字段拆分为 `备注1` 和 `备注2`。
- 旧版 v1/v2/v3/v4 工作簿首次打开时会自动备份并升级；原 `备注` 内容会迁移到 `备注1`，`备注2` 保持空白。
- 新增/编辑设备窗口中 `备注1` 和 `备注2` 改为更高的多行输入框，可直接查看和填写更多内容。
- 右侧设备详情新增“备注信息”分组，分别展示 `备注1` 与 `备注2`。
- 搜索范围同步包含 `备注1` 和 `备注2`，备注修改也会按字段写入变化历史。
- 变化记录改为按同一次保存聚合的彩色卡片，相邻卡片使用不同浅色背景，便于区分多次修改。
- 主界面新增 `复制` 功能，可基于相近设备生成新记录；保存后自动选中新设备，并用浅黄色高亮提示本次复制结果。
- 设备详情顶部新增“资产概览”摘要区，集中展示当前状态、位置、归属、金额、标签和存储介质数量。

### 2026-06-19：界面与录入细节优化

- 进一步加强选中项样式，覆盖列表、表格、树和下拉弹出项，避免 Windows 默认深紫色导致文字不清晰。
- 新增/编辑设备窗口恢复为 `一级分类`、`二级分类` 两个下拉框，保存时仍自动维护完整 `类别路径`。
- 添加存储介质时，`容量数值` 默认保持空白，不再预设 `0.00`。
- `是否已打印标签` 调整为更醒目的标签状态提示，未打印时使用浅黄色提醒，降低漏选概率。
- 新增/编辑窗口统一所有分组的标签列宽、控件高度和边距，字段标签改为靠左排列，输入框起点和右侧边界保持一致。
- 右侧设备详情改为等宽浅色分组卡片式展示，身份、产品、资产、管理、存储介质和系统信息之间更容易区分。

### 2026-06-19：设备台账工作台 2.0

- 工作簿升级为版本 4，资产台账新增 `类别路径`，支持三级及更多级设备分类，同时保留 `一级类别` 和 `二级类别` 方便直接查看 Excel。
- `存储介质` 新增 `使用状态` 字段，介质新增、修改、移除和状态变化都会写入变化历史。
- 设置页的设备分类和位置支持任意层级子项，并增加删除功能；只有未被设备使用且没有子项的分类或位置可以物理删除。
- 新增/编辑设备时，分类改为完整路径选择，`存放地点` 和 `取得方式` 支持下拉选择或手动输入。
- `等级` 调整为建议填写项，为空保存时会提醒一次，但仍允许继续保存。
- 主界面列表新增 `序号` 列，筛选或搜索后从 1 重新排列；右侧详情按信息组展示，并显示存储介质状态。
- 调整工作台视觉风格，选中设备使用浅蓝整行高亮，减少系统默认紫色选中导致的阅读问题。
- 旧版 v1/v2/v3 工作簿首次打开时会自动备份并升级到版本 4。

### 2026-06-16：资产UID与业务资产标识符拆分

- 工作簿升级为版本 3，将系统自动生成的 `AST-年份-序号` 字段重命名为 `资产UID`。
- 在 `bm编码` 后新增可手动填写的 `资产唯一标识符` 字段，用于记录业务台账、标签或外部系统中的资产标识。
- `资产唯一标识符` 可留空；填写后必须唯一，修改时会进入字段级变化历史。
- 存储介质和变更历史中的设备关联字段同步改为 `资产UID`，减少和业务标识符混淆。
- 旧版工作簿首次打开时会自动备份并升级到新版字段结构。

### 2026-06-14：内存缓存、同步保护与应用图标

- 启动或手动刷新时一次性读取并校验 Excel；搜索、筛选、详情和变化历史改为查询内存缓存。
- 每次成功写入仍保留时间戳备份，并从正式 Excel 重新加载缓存。
- 增加工作簿文件指纹检测；程序运行期间 Excel 被外部修改时，禁止覆盖并提示刷新。
- 缓存刷新失败时保留最后一次有效数据，并禁止继续保存，避免重复新增或覆盖数据。
- 为 Windows EXE、程序窗口和任务栏增加统一的设备台账图标。

### 2026-06-14：设备字段与存储介质升级

- 工作簿升级为版本 2，扩展设备器材、产品、取得、管理、使用、等级和标签等字段。
- 新增独立的 `存储介质` 工作表，一台设备可记录任意数量的 SSD、机械硬盘或其他介质。
- 仅设备器材名称必填；bm 编码允许为空，但非空时必须唯一。
- 增加管理人、使用人筛选，存储介质搜索和介质变化历史。
- 下拉框与数字输入框禁止鼠标滚轮误修改。

### 2026-06-07：首个可用版本

- 提供设备新增、编辑、搜索、筛选、详情和字段级变化追踪。
- 使用单个 Excel 工作簿保存业务数据，并支持写入前备份和原子保存。
- 提供 PySide6 桌面界面和 PyInstaller Windows 文件夹版打包流程。

## 核心能力

- 每台设备拥有独立的 `AST-年份-序号` 资产UID，由系统自动生成且不可手动修改。
- `资产唯一标识符` 是可手动填写的业务字段，可为空；填写后必须保持唯一。
- 新增、编辑、搜索、排序以及按多级类别、使用状态、存放地点、品牌、使用人筛选。
- 仅设备器材名称必填；bm 编码可为空，填写后必须保持唯一。
- 管理部门、管理人、使用部门、使用人支持从已有台账选择或直接输入。
- 设备支持 `备注1` 和 `备注2` 两段备注，旧版工作簿中的原备注会兼容迁移到 `备注1`。
- 一台设备支持记录任意数量的 SSD、机械硬盘或其他存储介质，并可单独记录介质使用状态。
- 修改使用状态、存放地点、金额和存储介质等内容时自动记录修改前后值。
- 变化记录支持按单个变化字段查看；保存后立即刷新并展示最新历史。
- 中央设备列表支持右键快捷操作，可直接查看详情、查看变化记录、编辑、复制、删除或刷新。
- 启动时一次性读取 Excel 到内存，搜索、筛选、详情和历史查询不重复打开工作簿。
- 每次写入前自动备份，保存时使用临时文件原子替换。
- 检测程序运行期间的外部 Excel 修改，发现冲突时禁止覆盖并要求刷新。
- 设置页维护多级分类、存放地点层级、使用状态、取得方式和当前操作者；未使用的叶子分类或位置可以删除。
- Excel 文件可直接打开查看，正式修改建议通过程序完成。

## 运行

需要 Python 3.11 或更高版本。

```powershell
python -m pip install -r requirements.txt
.\run_asset_ledger.bat
```

也可以直接运行：

```powershell
$env:PYTHONPATH="src"
python -m asset_ledger.app
```

首次启动会在项目目录下创建 `data/设备资产台账.xlsx`。之后可以在设置页切换到其他工作簿。

## 日常使用

1. 点击“新增设备”，填写设备器材名称和需要记录的其他信息。
2. 对相近设备可先选中已有设备，再点击“复制”，微调后保存生成新设备。
3. 在主列表使用搜索框或左侧条件查找设备。
4. 选择设备后，右侧可查看完整信息和变化记录。
5. 也可以在设备列表中右键设备行，快速查看详情、查看变化记录、编辑、复制、删除或刷新。
6. 双击设备或点击“编辑”修改；填写修改说明可帮助以后追溯。
7. 删除设备会同时删除该设备的存储介质和变化记录；确认前请核对弹窗中的设备信息。
8. 在“设置”中维护分类、存放地点、使用状态、取得方式和当前操作者。

状态为停用的分类或存放地点不会再出现在新建设备选项中，但已有设备记录仍保留并可查看。

程序运行期间的数据查询来自最近一次成功加载的内存缓存。若使用 Excel 或其他程序直接
修改了工作簿，请回到台账程序点击“刷新”后再继续保存；检测到外部修改时，程序会禁止
保存，避免覆盖外部内容。

## 数据安全与同步

- Excel 是唯一正式数据源，内存缓存只用于加速查询。
- 程序启动、点击“刷新”或成功保存后，会从正式 Excel 重建完整缓存。
- 每次实际写入前都会创建备份；设备编辑未发生内容变化时不会执行写入或创建备份。
- 删除设备前也会创建备份；删除后当前工作簿不再保留该设备及其历史，如需找回请从备份恢复。
- 保存期间使用临时文件并原子替换正式工作簿，降低中断导致文件损坏的风险。
- 如果程序检测到 Excel 被外部修改、删除、移动或占用，会停止保存并显示原因。
- 如果提示“Excel 已保存，但缓存刷新失败”，请勿重复新增或编辑；先点击“刷新”或重新启动程序。

## Excel 工作簿

工作簿包含以下工作表：

- `资产台账`：每台设备一行，保存当前信息。
- `存储介质`：每块存储介质一行，通过资产UID关联设备，并保存介质使用状态。
- `变更历史`：只追加字段级历史记录，并通过资产UID关联设备。
- `分类字典`：支持任意层级的设备分类，资产台账用 `类别路径` 保存完整路径。
- `位置字典`：支持任意层级的位置。
- `枚举字典`：使用状态与取得方式选项。
- `系统配置`：工作簿版本、下一个资产序号和默认操作者。

每次写入前，程序会在工作簿旁的 `backups` 文件夹创建时间戳备份。

旧版工作簿第一次由新版程序打开时，会先自动备份，再升级字段结构。版本 4 会补充
`类别路径` 和 `存储介质.使用状态`；版本 5 会将原 `备注` 迁移为 `备注1`，并新增
`备注2`。升级过程中不要同时使用 Excel 打开该文件。

## 测试

标准库测试无需额外安装测试框架：

```powershell
$env:PYTHONPATH="src"
$env:QT_QPA_PLATFORM="offscreen"
python -m unittest discover -s tests -v
```

安装开发依赖后也可以运行：

```powershell
python -m pip install -r requirements-dev.txt
pytest
```

## 打包 Windows 文件夹版

文件夹版会生成在 `dist/设备资产台账/`。其中：

- `设备资产台账.exe`：程序入口。
- `data/设备资产台账.xlsx`：首次启动后自动创建的业务工作簿。
- `_internal/`：程序运行依赖，不要删除或单独移动 EXE。
- `使用说明.md`：项目使用说明。

EXE、程序窗口和任务栏图标来自 `assets/app_icon.ico`。替换图标后需要重新运行打包脚本。

首次打包前安装构建依赖：

```powershell
python -m pip install -r requirements-build.txt
```

然后双击 `build_windows.bat`，或在 PowerShell 中运行：

```powershell
.\build_windows.bat
```

打包完成后运行：

```powershell
.\dist\设备资产台账\设备资产台账.exe
```

首次运行会在 EXE 同级的 `data` 文件夹中创建 `设备资产台账.xlsx`。发布程序时应复制整个
`dist/设备资产台账/` 文件夹；仅转发台账时，可以单独转发 Excel 文件。不要把真实业务
Excel 放入待发布文件夹后再发送给无关人员。
